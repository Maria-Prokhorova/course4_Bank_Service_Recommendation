# src/main/resources/application-dev.properties

# ???????? ?????? (H2)
spring.datasource.url=jdbc:h2:mem:primary_db
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# ????????? ???????? ?????? (PostgreSQL)
spring.secondary-datasource.jdbc-url=${RULES_DB_URL:jdbc:postgresql://localhost:5432/dbForTest}
spring.secondary-datasource.username=${RULES_DB_USER:UserForTest}
spring.secondary-datasource.password=${RULES_DB_PASSWORD:123}
spring.secondary-datasource.driver-class-name=org.postgresql.Driver

# Liquibase ??? PostgreSQL
spring.liquibase.url=${LIQUIBASE_URL:jdbc:postgresql://localhost:5432/dbForTest}
spring.liquibase.user=${LIQUIBASE_USER:UserForTest}
spring.liquibase.password=${LIQUIBASE_PASSWORD:123}
spring.liquibase.driver-class-name=org.postgresql.Driver
spring.liquibase.change-log=classpath:/liquibase/changelog-master.yml
spring.liquibase.enabled=true

# ?????????????? ???? ??? ??????????
spring.jpa.hibernate.ddl-auto=update