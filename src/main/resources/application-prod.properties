# src/main/resources/application-prod.properties

# ????????? ???????? ?????? (PostgreSQL)
spring.secondary-datasource.jdbc-url=${RULES_DB_URL:jdbc:postgresql://your_host:5432/your_database}
spring.secondary-datasource.username=${RULES_DB_USER:db_username}
spring.secondary-datasource.password=${RULES_DB_PASSWORD:db_password}
spring.secondary-datasource.driver-class-name=org.postgresql.Driver

# Liquibase ??? PostgreSQL
spring.liquibase.url=${LIQUIBASE_URL:jdbc:postgresql://your_host:5432/your_database}
spring.liquibase.user=${LIQUIBASE_USER:db_username}
spring.liquibase.password=${LIQUIBASE_PASSWORD:db_password}
spring.liquibase.driver-class-name=org.postgresql.Driver
spring.liquibase.change-log=classpath:/liquibase/changelog-master.yml
spring.liquibase.enabled=true

# ?????????? ?????????????? ???? ??? ???????????????? ?????
spring.jpa.hibernate.ddl-auto=none